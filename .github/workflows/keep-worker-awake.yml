name: Keep worker awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping worker /health
        run: curl -fsS https://sistema-criacao-conteudo-worker.onrender.com/health
